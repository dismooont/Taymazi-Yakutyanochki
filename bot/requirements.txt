# Зависимости Telegram-бота.
#
# Ни torch, ни transformers, ни faiss здесь нет и быть не должно: бот не считает
# эмбеддинги и не открывает индекс — он обращается к API по HTTP (bot/client.py).
# Поэтому образ бота весит около 150 МБ вместо двух гигабайт, а модель CLIP
# существует в системе ровно в одном экземпляре — в API-процессе.

aiogram>=3.10,<4.0
aiohttp-socks>=0.8         # нужен aiogram для доступа к Telegram через прокси (TELEGRAM_PROXY)
httpx>=0.27                # клиент API
python-dotenv>=1.0
