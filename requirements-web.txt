# Зависимости веб-приложения (web/ + core/).
#
# Список намеренно самостоятельный, а не "-r requirements.txt": вебу не нужны
# matplotlib (визуализация только в CLI) и pandas (чтение CSV с подписями COCO),
# а в образе это лишние десятки мегабайт.
#
# torch ставится отдельно в Dockerfile.web (CPU-only сборка), здесь его нет —
# иначе подтянется тяжёлая CUDA-версия.
#
# Запуск в разработке:
#   pip install torch -r requirements-web.txt
#   uvicorn web.app:app --reload --port 8000
# Только один воркер: веса CLIP занимают ~600 МБ на процесс, а несколько воркеров
# писали бы в один FAISS-индекс наперегонки (docs/WEB_PLAN.md, раздел 2).

# --- веб ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9    # загрузка файлов и архивов
argon2-cffi>=23.1          # хеширование паролей (argon2id)
python-dotenv>=1.0         # чтение .env при локальном запуске (в Docker — env_file)

# --- ядро ---
transformers>=4.40,<5.0    # см. README: раздел "Известные проблемы" про transformers 5.x
faiss-cpu>=1.7.4
pillow>=10.0
numpy>=1.24
tqdm>=4.65
deep-translator>=1.11      # перевод текстовых запросов RU->EN
