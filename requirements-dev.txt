# Зависимости для запуска тестов: pip install -r requirements-dev.txt
# Тесты не грузят веса CLIP (см. tests/conftest.py), поэтому torch/transformers
# нужны только как импорты — они уже в requirements.txt.

-r requirements.txt

pytest>=8.0

# Только для scripts/eval_caption_path.py — замер пользы поиска через подписи.
# В requirements.txt намеренно не вынесено: ни веб, ни бот SBERT не используют.
sentence-transformers>=3.0
